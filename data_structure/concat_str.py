# join()连接可迭代对象
parts = ['Is', 'Chicago', 'Not', 'Chicago?']

print("-".join(parts))

a = 'Is Chicago'
b = 'Not Chicago?'

# format 拼接
print('{} {}'.format(a, b))

# 简单拼接两个字符串
c = "hello" "world"
print(c)